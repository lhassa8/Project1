[project]
name = "agent-runner"
version = "0.1.0"
description = "Native Claude tool-call loop with interception layer"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.39.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[project.scripts]
agent-runner = "main:main"
